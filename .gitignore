# Binaries
*.exe
*.dll
*.so
*.dylib
/backend/formula-service
/backend/cmd/server/server
# Stray server binary from `go build ./cmd/server` without -o bin/server
# (correct command is `go build -o bin/server ./cmd/server`, see Makefile)
/backend/server

# Database
*.db
*.db-journal
*.db-shm
*.db-wal

# IDE / AI
.idea/
.vscode/
.claude/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Go
/backend/vendor/

# Node
node_modules/
/frontend/dist/
/frontend/.env.local

# Environment
.env
.env.local
.env.production
backend/bin/
.gstack/

# Playwright
test-results/
# Playwright's scratch output inside our curated screenshot dirs
tests/screenshots/*/playwright-output/

# Prompt history — local-only record of user prompts, kept for the author
# but not checked into version control (may contain context that is not
# meaningful to other collaborators and grows every session)
prompt_history/

# Workflow marker
.codex-review-done
